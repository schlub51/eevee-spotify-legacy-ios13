# Local Artifacts

This directory is for local-only generated files during rebuilds.

Do not commit IPAs, decrypted app bundles, dylibs, `.deb` packages, entitlements extracted from apps, or temporary build workspaces.

